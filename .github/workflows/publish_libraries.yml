name: publish_libraries
on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  main:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      # Cache node_modules
      - uses: actions/setup-node@v3
        with:
          node-version: 20
          cache: 'npm'
      - run: npx run-many -t build --parallel=3
      - run: node devel/publish_libs.js
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}